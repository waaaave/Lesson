const initSettingsState= {
  showSettings: false,
  sidebarLogo: '', // logo 图片地址
  fixHeader: false,
  tagsView: []
}

export default function settings(state = initSettingsState, action) {
  switch (action.type) {
   
    default:
    return state
  }
}