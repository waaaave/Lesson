// import * as actionTypes from './constants';

const defaultstate = {
    category:[]
}

const reducer = (state = defaultstate, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}
export default reducer;